#pragma once
#include <vector>

namespace leetcode {
	int search_RotatedSortedArray(const std::vector<int>& nums, int target);
	int search_RotatedSortedDuplicateArray(const std::vector<int>& nums, int target);
}