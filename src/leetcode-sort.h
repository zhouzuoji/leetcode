#pragma once
#include <vector>

namespace leetcode {
	namespace sort {
		void sortColors(std::vector<int>& nums);
		void merge(std::vector<int>& nums1, int m, std::vector<int>& nums2, int n);
	}
}